<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wanderlust</title>
    <link rel="icon" href="/icon.png" type="image/x-icon">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      crossorigin="anonymous"
      referrerpolicy="no-referrer" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;700&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/rating.css" />
  </head>
  <body>
    <!-- CRITICAL FIXES: Define variables locally from res.locals -->
    <% 
        // 1. Define currUser locally (Fixes previous error)
        const currUser = locals.currUser || null; 

        // 2. Define flash messages locally (Fixes current error)
        const success = locals.success;
        const error = locals.error;
    %>

    <!-- Explicitly pass currUser -->
    <%- include("../includes/navbar.ejs", { currUser: currUser })%>

    <div class="container">
        <!-- Explicitly pass success and error (Fixes "success is not defined") -->
      <%- include("../includes/flash.ejs", { success: success, error: error }) %> <%- body %>
    </div>

    <%- include("../includes/footer.ejs")%>
    <script
      type="module"
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      crossorigin="anonymous"></script>
    <script type="module" src="/js/script.js"></script>
  </body>
</html>